services:
  - type: web
    name: password-checker-api
    runtime: native
    rootDir: backend
    buildCommand: ./mvnw -DskipTests package
    startCommand: java -jar target/password-checker-0.0.1-SNAPSHOT.jar
    envVars:
      - key: PORT
        value: "8088"
